{
  "id": "control-capture-healthy",
  "feature": "capture",
  "expect": "PASS",
  "defect": null,
  "detects": "control case — nothing is broken. An agent that fails this is crying wolf, which is worse than missing a bug because it trains everyone to ignore the harness."
}
